# AGENTS.md

## 목적
이 프로젝트는 KKBBQ 홈페이지를 안정적이고 일관되게 유지하기 위한 작업 지침을 담는다.

우선순위는 다음과 같다.

1. 깨진 한글 없이 정상 표시
2. HTML 구조와 링크 안정성 유지
3. 모바일 화면 품질 유지
4. 요청받은 범위만 정확하게 수정

## 작업 원칙

- 먼저 실제 파일 내용을 확인한 뒤 수정한다.
- 추측으로 구조를 바꾸지 않는다.
- HTML 구조, 클래스명, 링크 대상은 가능한 그대로 유지한다.
- 필요한 경우에만 최소 범위로 수정한다.
- 한 번에 많이 바꾸기보다, 검증 가능한 단위로 바꾼다.


## 작업 전 컨펌 규칙

아래 작업은 바로 수정하지 말고, 먼저 작업 범위와 방법을 보고한 뒤 사용자 컨펌을 받는다.

- 공통 CSS 변경
- 여러 HTML 페이지 동시 수정
- 헤더, 네비게이션, 푸터, 히어로 구조 변경
- 슬라이드쇼, 지도, CTA, 외부 링크에 영향을 줄 수 있는 변경
- 대체 이미지, 배경 이미지, 애니메이션 구조 변경

작업 전 보고에는 아래 항목을 포함한다.

- 수정 대상 파일
- 예상 변경 범위
- PC 화면 영향 여부
- 모바일 화면 영향 여부
- 되돌리는 방법
- 예상 리스크

단순 오타 수정, 문구 1~2개 교체, 명확한 색상·간격 수치 조정처럼 영향 범위가 작은 작업은 컨펌 없이 진행할 수 있다.

## 속도 및 토큰 절약 규칙

- 요청과 무관한 파일은 열지 않는다.
- 같은 구조가 반복되면 대표 파일 1개와 예외 파일만 비교한다.
- 전체 파일을 다시 설명하지 말고, 변경한 부분 중심으로 짧게 보고한다.
- 이미 확인한 사실을 반복해서 설명하지 않는다.
- 작업 보고는 긴 서술보다 파일명, 선택자, 변경값 중심으로 작성한다.
- 불필요한 리팩토링, 코드 정렬, 주석 추가는 요청이 있을 때만 한다.

## 백업 및 diff 확인 규칙

여러 파일을 동시에 수정하기 전에는 현재 상태를 보존한다.

가능하면 아래 순서로 확인한다.

1. `git status --short`
2. 수정 전 관련 파일 위치 확인
3. 수정 후 `git diff --name-only`
4. 수정 후 핵심 변경 내용만 `git diff`로 확인

Git을 사용할 수 없는 환경이면 원본 파일을 `.bak`로 복사하지 말고, 변경 내용을 최소화하고 보고에 되돌리는 방법을 명확히 적는다.

배포용 ZIP이나 사용자 전달용 ZIP을 만들 때는 `.git`, `.vscode`, `node_modules`, 기존 ZIP, 임시 파일을 포함하지 않는다.

## 파일 범위

주요 수정 대상은 아래 파일들이다.

- `index.html`
- `menu.html`
- `reservation.html`
- `reviews.html`
- `instagram.html`
- `contact.html`
- `assets/css/kbbq-custom.css`

공통 헤더, 네비게이션, 푸터, 히어로, CTA는 여러 페이지에 반복되므로 한 파일만 보고 판단하지 않는다.

## 인코딩 규칙

- 모든 HTML, CSS 파일은 UTF-8로 저장한다.
- 한글이 깨져 보이면 먼저 인코딩 문제를 의심한다.
- 저장 후에는 브라우저나 파일 읽기 결과에서 실제 한글 표시를 확인한다.
- `\`r\`n` 같은 문자 리터럴이 텍스트로 들어가지 않도록 주의한다.

## HTML 수정 규칙

- 텍스트 수정과 구조 수정은 구분한다.
- 요청이 텍스트 수정이라면 문구만 바꾸고 태그 구조는 그대로 둔다.
- 닫는 태그 오류가 보이면 해당 블록 범위 안에서만 바로잡는다.
- 공통 네비게이션은 전 페이지에서 동일한 항목과 순서를 유지한다.
- 새 클래스명은 만들기 전에 기존 클래스가 이미 있는지 먼저 확인한다.

## CSS 수정 규칙

- CSS는 필요한 경우에만 수정한다.
- 모바일 수정은 기본적으로 `@media screen and (max-width: 980px)` 기준으로 검토한다.
- PC 디자인을 건드리지 않는 것을 기본 원칙으로 한다.
- 히어로, 오버레이, 타이틀 크기, CTA 여백은 모바일에서 우선 확인한다.
- 중복된 선택자나 `!important`가 있으면 실제 우선순위를 확인하고 수정한다.

## 이 프로젝트의 주요 클래스

### index.html 히어로
- `.kbbq-hero`
- `.kbbq-hero-slider`
- `.kbbq-hero-bg`
- `.kbbq-hero-inner`

### 서브 페이지 히어로
- `.kbbq-subhero`

### 오버레이 관련
- `.kbbq-hero::before`
- `.kbbq-hero-bg::after`
- `.kbbq-subhero::after`

## 검증 규칙

수정 후에는 아래를 확인한다.

- 모든 주요 페이지가 열리는지
- 네비게이션 링크가 정상 작동하는지
- 한글이 깨지지 않는지
- 태그가 닫혔는지
- 모바일에서 히어로가 너무 낮지 않은지
- 오버레이가 너무 진하지 않은지
- CTA 버튼이 눌리는지

가능하면 다음 순서로 확인한다.

1. `index.html`
2. `menu.html`
3. `reservation.html`
4. `reviews.html`
5. `instagram.html`
6. `contact.html`

## 수정 보고 형식

작업 후에는 아래 형식으로 간단히 보고한다.

- 수정한 파일 목록
- 변경한 핵심 문구 또는 스타일
- 문제였던 원인
- 변경 전 값
- 변경 후 값
- PC 영향 여부
- 모바일 영향 여부
- 남은 리스크
- 되돌리는 방법

## 금지 사항

- 추측만으로 대규모 치환하기
- 원본 확인 없이 구조 변경하기
- 불필요하게 PC 스타일까지 바꾸기
- 무단 삭제, 강제 초기화, 되돌리기
- 확인 없이 네비게이션이나 히어로 구조를 바꾸기

## 예외 처리

- 파일이 깨져 보이면 먼저 인코딩과 실제 저장 상태를 확인한다.
- 동일한 문제가 여러 페이지에 반복되면 공통 원인을 먼저 찾는다.
- 한 페이지만 고쳤는데 다른 페이지가 깨졌다면 공통 헤더/CSS를 먼저 의심한다.
