# AGENTS.md

## 목적
이 프로젝트는 KKBBQ 홈페이지를 안정적이고 일관되게 유지하기 위한 작업 지침을 담는다.

우선순위는 다음과 같다.

1. 깨진 한글 없이 정상 표시
2. HTML 구조와 링크 안정성 유지
3. 모바일 화면 품질 유지
4. 요청받은 범위만 정확하게 수정

## 작업 원칙

- 먼저 실제 파일 내용을 확인한 뒤 수정한다.
- 추측으로 구조를 바꾸지 않는다.
- HTML 구조, 클래스명, 링크 대상은 가능한 그대로 유지한다.
- 필요한 경우에만 최소 범위로 수정한다.
- 한 번에 많이 바꾸기보다, 검증 가능한 단위로 바꾼다.

## 고객 중심 콘텐츠 작성 규칙

홈페이지의 모든 문구는 실제 방문 고객이 읽는다는 전제로 작성한다.

- 개발자 관점 설명은 쓰지 않는다.
- 고객이 얻는 경험과 느낌을 먼저 말한다.
- 안내 문구는 쉽고 자연스럽게 읽히도록 쓴다.
- 문구에 개발자에게 설명하는 표현이 보이면 고객 언어로 다시 쓴다.

아래 표현은 사용하지 않는다.

- HTML 구조
- 카드형 구성
- CSS
- 모바일 최적화
- 검색엔진
- SEO
- 데이터
- 시스템
- 구조
- 알고리즘
- 구현
- 자동화
- 플랫폼

문구 작성 우선순위는 다음 순서로 둔다.

1. 분위기
2. 맛
3. 편의성
4. 가족 외식
5. 단체 모임
6. 예약 편의성

예시는 다음과 같이 쓴다.

- 잘못된 표현: "검색 노출과 모바일 가독성을 위해 HTML 카드형으로 구성했습니다."
- 올바른 표현: "메뉴와 가격을 한눈에 확인할 수 있도록 보기 쉽게 정리했습니다."
- 잘못된 표현: "검색엔진이 메뉴명을 정확하게 읽을 수 있습니다."
- 올바른 표현: "처음 방문하시는 분도 메뉴를 쉽게 고를 수 있습니다."

## 예약 전환 중심 문구 작성 규칙

고객용 문구는 단순 안내보다 방문 이유를 만들어야 한다.

문장 우선순위는 다음 순서로 둔다.

1. 고객 경험
2. 분위기
3. 가족 외식
4. 단체 모임
5. 편의성
6. 예약 유도

단순 설명형 문장은 가능하면 경험형 문장으로 바꾼다.

예시는 다음과 같이 쓴다.

- 보통 문장: "메뉴와 가격을 확인하실 수 있습니다."
- 권장 문장: "가족 외식부터 모임까지 인원에 맞는 메뉴를 편하게 선택하실 수 있습니다."
- 보통 문장: "주소와 전화번호를 확인하실 수 있습니다."
- 권장 문장: "처음 방문하시는 분도 길 찾기와 문의를 편하게 하실 수 있습니다."
- 보통 문장: "후기를 확인하실 수 있습니다."
- 권장 문장: "실제 방문 고객이 느낀 분위기와 만족도를 먼저 확인해보세요."

고객이 행동하게 만드는 문장을 우선 사용한다.


## 작업 전 컨펌 규칙

아래 작업은 바로 수정하지 말고, 먼저 작업 범위와 방법을 보고한 뒤 사용자 컨펌을 받는다.

- 공통 CSS 변경
- 여러 HTML 페이지 동시 수정
- 헤더, 네비게이션, 푸터, 히어로 구조 변경
- 슬라이드쇼, 지도, CTA, 외부 링크에 영향을 줄 수 있는 변경
- 대체 이미지, 배경 이미지, 애니메이션 구조 변경

작업 전 보고에는 아래 항목을 포함한다.

- 수정 대상 파일
- 예상 변경 범위
- PC 화면 영향 여부
- 모바일 화면 영향 여부
- 되돌리는 방법
- 예상 리스크

단순 오타 수정, 문구 1~2개 교체, 명확한 색상·간격 수치 조정처럼 영향 범위가 작은 작업은 컨펌 없이 진행할 수 있다.

## 속도 및 토큰 절약 규칙

- 요청과 무관한 파일은 열지 않는다.
- 같은 구조가 반복되면 대표 파일 1개와 예외 파일만 비교한다.
- 전체 파일을 다시 설명하지 말고, 변경한 부분 중심으로 짧게 보고한다.
- 이미 확인한 사실을 반복해서 설명하지 않는다.
- 작업 보고는 긴 서술보다 파일명, 선택자, 변경값 중심으로 작성한다.
- 불필요한 리팩토링, 코드 정렬, 주석 추가는 요청이 있을 때만 한다.

## 백업 및 diff 확인 규칙

여러 파일을 동시에 수정하기 전에는 현재 상태를 보존한다.

가능하면 아래 순서로 확인한다.

1. `git status --short`
2. 수정 전 관련 파일 위치 확인
3. 수정 후 `git diff --name-only`
4. 수정 후 핵심 변경 내용만 `git diff`로 확인

Git을 사용할 수 없는 환경이면 원본 파일을 `.bak`로 복사하지 말고, 변경 내용을 최소화하고 보고에 되돌리는 방법을 명확히 적는다.

배포용 ZIP이나 사용자 전달용 ZIP을 만들 때는 `.git`, `.vscode`, `node_modules`, 기존 ZIP, 임시 파일을 포함하지 않는다.

## 파일 범위

주요 수정 대상은 아래 파일들이다.

- `index.html`
- `menu.html`
- `reservation.html`
- `reviews.html`
- `instagram.html`
- `contact.html`
- `assets/css/kbbq-custom.css`

공통 헤더, 네비게이션, 푸터, 히어로, CTA는 여러 페이지에 반복되므로 한 파일만 보고 판단하지 않는다.

## 인코딩 규칙

- 모든 HTML, CSS 파일은 UTF-8로 저장한다.
- 한글이 깨져 보이면 먼저 인코딩 문제를 의심한다.
- 저장 후에는 브라우저나 파일 읽기 결과에서 실제 한글 표시를 확인한다.
- `\`r\`n` 같은 문자 리터럴이 텍스트로 들어가지 않도록 주의한다.

## HTML 수정 규칙

- 텍스트 수정과 구조 수정은 구분한다.
- 요청이 텍스트 수정이라면 문구만 바꾸고 태그 구조는 그대로 둔다.
- 닫는 태그 오류가 보이면 해당 블록 범위 안에서만 바로잡는다.
- 공통 네비게이션은 전 페이지에서 동일한 항목과 순서를 유지한다.
- 새 클래스명은 만들기 전에 기존 클래스가 이미 있는지 먼저 확인한다.

## CSS 수정 규칙

- CSS는 필요한 경우에만 수정한다.
- 모바일 수정은 기본적으로 `@media screen and (max-width: 980px)` 기준으로 검토한다.
- PC 디자인을 건드리지 않는 것을 기본 원칙으로 한다.
- 히어로, 오버레이, 타이틀 크기, CTA 여백은 모바일에서 우선 확인한다.
- 중복된 선택자나 `!important`가 있으면 실제 우선순위를 확인하고 수정한다.

## 이 프로젝트의 주요 클래스

### index.html 히어로
- `.kbbq-hero`
- `.kbbq-hero-slider`
- `.kbbq-hero-bg`
- `.kbbq-hero-inner`

### 서브 페이지 히어로
- `.kbbq-subhero`

### 오버레이 관련
- `.kbbq-hero::before`
- `.kbbq-hero-bg::after`
- `.kbbq-subhero::after`

## 검증 규칙

수정 후에는 아래를 확인한다.

- 모든 주요 페이지가 열리는지
- 네비게이션 링크가 정상 작동하는지
- 한글이 깨지지 않는지
- 태그가 닫혔는지
- 모바일에서 히어로가 너무 낮지 않은지
- 오버레이가 너무 진하지 않은지
- CTA 버튼이 눌리는지

가능하면 다음 순서로 확인한다.

1. `index.html`
2. `menu.html`
3. `reservation.html`
4. `reviews.html`
5. `instagram.html`
6. `contact.html`

## 수정 보고 형식

작업 후에는 아래 형식으로 간단히 보고한다.

- 수정한 파일 목록
- 변경한 핵심 문구 또는 스타일
- 문제였던 원인
- 변경 전 값
- 변경 후 값
- PC 영향 여부
- 모바일 영향 여부
- 남은 리스크
- 되돌리는 방법

## 금지 사항

- 추측만으로 대규모 치환하기
- 원본 확인 없이 구조 변경하기
- 불필요하게 PC 스타일까지 바꾸기
- 무단 삭제, 강제 초기화, 되돌리기
- 확인 없이 네비게이션이나 히어로 구조를 바꾸기

## 예외 처리

- 파일이 깨져 보이면 먼저 인코딩과 실제 저장 상태를 확인한다.
- 동일한 문제가 여러 페이지에 반복되면 공통 원인을 먼저 찾는다.
- 한 페이지만 고쳤는데 다른 페이지가 깨졌다면 공통 헤더/CSS를 먼저 의심한다.

## 고객 노출 콘텐츠 최종 검수 규칙

홈페이지는 개발자를 위한 작업 보고서가 아니라 고객이 실제로 보고 예약과 방문을 결정하는 영업 공간이다.

따라서 고객이 실제로 보게 되는 영역에는 개발자 설명, SEO 설명, HTML/CSS 설명, 구조 설명, 시스템 설명을 남기지 않는다.

### 고객 페이지에서 사용하지 않는 표현

아래 단어와 표현은 고객 노출 문구에서 사용하지 않는다.

```text
HTML
CSS
JavaScript
SEO
검색엔진
데이터
시스템
구조
알고리즘
구현
자동화
플랫폼
모바일 최적화
카드형 구성
슬라이드 구조
렌더링
컴포넌트
```

### 잘못된 표현과 올바른 표현

잘못된 표현:

```text
검색 노출과 모바일 가독성을 위해 HTML 카드형으로 구성했습니다.
```

올바른 표현:

```text
메뉴와 가격을 한눈에 확인할 수 있도록 보기 쉽게 정리했습니다.
```

잘못된 표현:

```text
검색엔진이 메뉴명을 정확하게 읽을 수 있습니다.
```

올바른 표현:

```text
처음 방문하시는 분도 메뉴를 쉽게 고를 수 있습니다.
```

잘못된 표현:

```text
모바일 최적화를 적용했습니다.
```

올바른 표현:

```text
휴대폰에서도 편하게 확인하실 수 있습니다.
```

잘못된 표현:

```text
슬라이드 구조를 개선했습니다.
```

올바른 표현:

```text
사진으로 매장 분위기를 먼저 둘러보실 수 있습니다.
```

### 고객 관점 문구 작성 기준

문구는 아래 질문을 기준으로 점검한다.

1. 고객이 이 문장을 읽고 방문하고 싶어지는가?
2. 고객이 궁금한 정보를 쉽게 알려주는가?
3. 개발자에게 설명하는 문장이 남아 있지 않은가?
4. 가족 외식, 단체 모임, 분위기, 맛, 편의성, 예약 편의성 중 하나 이상을 전달하는가?

### 작업 완료 전 필수 검색

수정 완료 후 고객이 실제 보는 HTML 영역에서 아래 단어를 검색한다.

```text
HTML
CSS
SEO
검색엔진
데이터
시스템
구조
구현
자동화
플랫폼
모바일 최적화
카드형
렌더링
컴포넌트
```

발견되면 삭제하지 말고 고객 언어로 교체한다.

### 보고 규칙

고객 관점 문구 검수 후에는 아래를 짧게 보고한다.

```text
고객 관점이 아닌 문장 발견 수:
교체한 문장 수:
교체 전 예시:
교체 후 예시:
남은 의심 문장:
```


## Codex 자동 승인 및 백업 규칙

### 작업 승인 정책

읽기 전용 작업은 사용자 확인 없이 진행한다.

허용 작업:

- Get-Content
- Select-String
- dir
- ls
- tree
- git status
- git diff
- git diff --name-only
- git diff --stat
- 파일 참조 검색
- 링크 참조 검색

### 자동 백업 규칙

여러 파일을 동시에 수정하거나 삭제하기 전에는 현재 상태를 백업한다.

백업 위치:

프로젝트 상위 폴더

백업 폴더명 형식:

프로젝트명_backup_YYYYMMDD_HHMMSS

예시:

kkbbq_backup_20260608_023000

백업 후에만 수정 및 삭제를 진행한다.

### 자동 진행 가능한 작업

아래 작업은 백업 후 사용자 추가 승인 없이 진행한다.

- HTML 수정
- CSS 수정
- JavaScript 수정
- 사용하지 않는 이미지 삭제
- 사용하지 않는 CSS 삭제
- 사용하지 않는 JS 삭제
- 사용하지 않는 HTML 삭제
- sitemap.xml 수정
- robots.txt 수정
- README 수정
- AGENTS.md 수정
- 공통 컴포넌트 정리
- 플로팅 버튼 구조 개선
- 링크 수정
- 오타 수정

### 반드시 사용자 확인이 필요한 작업

아래 작업은 절대 자동 진행하지 않는다.

- git reset --hard
- git clean -fd
- Remove-Item -Recurse .git
- 상위 폴더 삭제
- 프로젝트 폴더 전체 삭제
- 드라이브 루트 삭제
- 복구 불가능한 강제 초기화
- 외부 API 키 삭제
- 환경설정 파일 전체 교체

### 작업 완료 보고

작업 완료 후 반드시 아래를 보고한다.

- 백업 폴더 경로
- 수정 파일 목록
- 삭제 파일 목록
- git diff --name-only
- git diff --stat
- 핵심 변경 내용

### 유지보수 원칙

사용자가 자리를 비운 상태에서도

- 읽기
- 검사
- 백업
- 일반 수정
- 불필요 파일 정리

는 자동 진행한다.

단, 복구가 어려운 작업은 반드시 사용자 확인을 받는다.
